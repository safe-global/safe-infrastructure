{{- $releaseName := .Release.Name }} # define the local var here

{{- range $chain, $chainValues := .Values.chains }}
{{ if $chainValues.enabled }}
---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ $releaseName }}-{{ $chainValues.network_name | lower }}-txs-worker-notifications-webhooks

spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $releaseName }}-{{ $chainValues.network_name | lower }}-txs-worker-notifications-webhooks
  template:
    metadata:
      labels:
        app: {{ $releaseName }}-{{ $chainValues.network_name | lower }}-txs-worker-notifications-webhooks
    spec:
      containers:
        - name: {{ $chainValues.network_name | lower }}-txs-worker-notifications-webhooks
          image: safeglobal/safe-transaction-service:latest
          command: ["docker/web/celery/worker/run.sh"]
          env:
            - name: ETHEREUM_NODE_URL
              value: {{ $chainValues.node_url }}
            - name: WORKER_QUEUES
              value: notifications,webhooks
          envFrom:
            - configMapRef:
                name: {{ $releaseName }}-{{ $chainValues.network_name | lower }}-txs-env

{{- end }} # if
{{- end }} # range