---
apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: {{ .Release.Name }}-nginx-shared-txs

spec:
  storageClassName: {{ .Release.Name }}-ebs-retain-volumes
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
